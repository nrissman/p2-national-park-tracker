{% layout 'layout.liquid' %}
{% block content %}
    <div class="container-md">
        <article>

            <h2>{{park.name}}</h2>
            <h3>
                {% if park.readyToEat %}
                    Ripe and ready to eat
                {% else %}
                    Not ripe, wait to eat it
                {% endif %}
            </h3>
            <h4>Color: {{park.color}}</h4>
            <div class="btn-group">

                <a href="/parks/{{park._id}}/edit" class="btn btn-primary">Edit Park</a>
                <form action="/parks/{{park._id}}?_method=DELETE" method="POST">
                    <input type="submit" value="Delete" class="btn btn-danger">
                </form>
            </div>
        </article>
        <div class="col-md-6">
            <form action="/comments/{{park._id}}" method="POST">
                <fieldset>
                    <legend>Add comment</legend>
                    <label class="form-label">Comment: </label>
                    <input type="text" class="form-control" name="note" placeholder="Enter comment here"/>
                </fieldset>
                <input type="submit" class="btn btn-success" value="Add comment" />
            </form>
        </div>
        {% if park.comments.length > 0 %}
        <div class="container-sm justify-content-center">
            <div class="col-md-8 col-lg-6" >
                {% for comment in park.comments %}
                {% render '../comment.liquid' comment: comment, userId: userId, park: park, username: username%}
                {% endfor %}
            </div>
        </div>
        {% endif %}
    </div>
{% endblock %}